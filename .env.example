# Backend API base for the free-trial button (POST ${VITE_API_URL}/api/trial).
# Defaults to the production API if unset. Set per-deploy (e.g. on Vercel/Netlify).
VITE_API_URL=https://sales-scraper.daddy-leads.com
