// The Ledger aesthetic is paper + grain + ink — no ambient gradient blobs.
// (Blurred aurora blooms are the fastest way to look template/AI-made.)
// Kept as a component so Layout's import stays stable.
export default function BackgroundFX() {
  return null
}
